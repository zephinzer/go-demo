export HELM_HOME=$(pwd)/.helm;
export HELM_TLS_CA_CERT=$(pwd)/.helm/ca.cert.pem;
export HELM_TLS_CERT=$(pwd)/.helm/helm.cert.pem;
export HELM_TLS_KEY=$(pwd)/.helm/helm.key.pem;
export HELM_TLS_ENABLE=1;
# export HELM_TLS_VERIFY=1;
export TILLER_NAMESPACE=gitlab;
