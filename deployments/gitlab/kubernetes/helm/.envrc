export HELM_HOME=$(pwd)/.helm;
export HELM_TLS_CA_CERT=$(pwd)/.ssl/ca.cert.pem;
export HELM_TLS_CERT=$(pwd)/.ssl/helm.cert.pem;
export HELM_TLS_KEY=$(pwd)/.ssl/helm.key.pem;
export HELM_TLS_ENABLE=1;
export HELM_TLS_VERIFY=1;
export HELM_TLS_HOSTNAME=tiller;
export TILLER_NAMESPACE=gitlab;
